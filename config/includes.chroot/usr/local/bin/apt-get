#!/bin/bash

# Allow root to use apt-get (needed for system scripts)
if [ "$EUID" -eq 0 ]; then
    /usr/bin/apt-get "$@"
    exit $?
fi

echo "🐶 Woof! This is FetchOS — we don't use 'apt-get' here!"
echo ""
echo "   Did you mean: woof $@"
echo ""
echo "   Run 'woof' to get started!"
exit 1
